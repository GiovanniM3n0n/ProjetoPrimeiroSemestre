var typed = new Typed(".input",{
    strings:["Bem vindo"],
    typeSpeed: 60,
    backSpeed: 60,
    loop: false,
    
});
var typed = new Typed(".oi",{
    strings:["Entre em contato comigo:"],
    typeSpeed: 60,
    backSpeed: 60,
    loop: false,
    startDelay: 1000 
});
